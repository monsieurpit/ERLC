# Disabled Replit configuration to prevent automatic runs or creating multiple workspaces.
# This repository is intended to be deployed on Railway only. Do not auto-run on Replit.

# modules intentionally left empty to avoid Replit auto-detection
modules = []

[workflows]
runButton = ""

[agent]
expertMode = false
stack = ""

# NOTE: If you want to re-enable Replit, restore the original .replit file from git history.
