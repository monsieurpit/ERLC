# Removed: api-react

This folder has been intentionally removed from active deployment to ensure the repository only produces a single deployable service (api-server).

Do not deploy this package. To restore it, revert the commit that neutralized this folder.
